% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{convertbands}
\alias{convertbands}
\title{Convert RGB bands to a named colour space}
\usage{
convertbands(R, G, B, space, ...)
}
\arguments{
\item{R, G, B}{Numeric matrices of the same dimensions, sRGB on the 0-255
scale. Integer matrices are accepted and converted.}

\item{space}{One of [available_spaces()].}

\item{...}{Passed to the conversion. Only [rgb_to_cam02()] takes extra
arguments (`L_A`, `Y_b`, `surround`, `whitepoint`); supplying them for
any other space is an error rather than a silent no-op.}
}
\value{
A named list of matrices. The names are the component names for
  that space — `L`, `a`, `b` for `"lab"`, and so on — which is also what
  GeoPalette returns alongside its arrays.
}
\description{
A dispatcher over every `rgb_to_*` function, for when the target space is
chosen at runtime.
}
\examples{
R <- matrix(c(255, 0, 128), 1, 3)
G <- matrix(c(255, 0, 64), 1, 3)
B <- matrix(c(255, 0, 32), 1, 3)
names(convertbands(R, G, B, "lab"))
names(convertbands(R, G, B, "dlab"))
}
