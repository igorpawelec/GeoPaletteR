% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{convert_raster}
\alias{convert_raster}
\title{Convert a raster to a colour space and write it out}
\usage{
convert_raster(input, output = NULL, space = "lab", ...)
}
\arguments{
\item{input}{A `SpatRaster` or a path to an RGB raster.}

\item{output}{Path for the multi-band output raster. If `NULL`, nothing
is written and the components are returned only.}

\item{space}{One of [available_spaces()].}

\item{...}{Passed to the conversion; see [convertbands()].}
}
\value{
Invisibly, the named list of component matrices.
}
\description{
Convert a raster to a colour space and write it out
}
\details{
Band names in the output are the component names of the space, so a
file written here is self-describing and lines up with what GeoPalette
writes for the same scene.

Requires \pkg{terra}.
}
\examples{
\donttest{
if (requireNamespace("terra", quietly = TRUE)) {
  r <- terra::rast(nrows = 4, ncols = 4, nlyrs = 3, vals = runif(48, 0, 255))
  out <- convert_raster(r, output = NULL, space = "lab")
  names(out)
}
}
}
