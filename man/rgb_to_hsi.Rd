% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hue.R
\name{rgb_to_hsi}
\alias{rgb_to_hsi}
\title{RGB to HSI}
\usage{
rgb_to_hsi(R, G, B)
}
\arguments{
\item{R, G, B}{Numeric matrices of the same dimensions, sRGB on the 0-255
scale. Integer matrices are accepted and converted.}
}
\value{
A named list of three matrices: `H` (0-360 degrees), `S` (per
  cent, 0-100) and `I` (0-255).
}
\description{
RGB to HSI
}
\details{
Note the scales, which differ from [rgb_to_hsl()] and [rgb_to_hsv()]:
saturation is a percentage and intensity keeps the 0-255 range of the
input rather than being normalised.
}
\examples{
R <- matrix(c(255, 10), 1, 2); G <- matrix(c(0, 10), 1, 2)
B <- matrix(c(0, 10), 1, 2)
rgb_to_hsi(R, G, B)$S
}
